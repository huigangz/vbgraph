# P0.4d — Derived `ConfidenceTier` — Work Log

**Status:** Done (delivered together with P0.4's `src/types.ts` changes).
**Date:** 2026-05-20

## Scope delivered

`src/types.ts` gained the derived confidence tier — no schema change, a pure function:

- `type ConfidenceTier = 'compiler' | 'syntactic' | 'scope-resolved' | 'inferred' | 'ambiguous'`.
- `deriveConfidenceTier(prov: GraphProvenance | undefined): ConfidenceTier`:
  - `scip` / `scip:external` -> `compiler`
  - `tree-sitter` / `tree-sitter (scip-empty-fallback)` -> `syntactic`
  - `scope-resolved` -> `scope-resolved`
  - `heuristic` / `framework:*` -> `inferred`
  - `undefined` -> `ambiguous`

## Notes

Implemented in the same `src/types.ts` edit as P0.4's provenance-ranking helpers because
they sit together and share the `GraphProvenance` union. The continuous `confidence`
column is untouched — `ConfidenceTier` is a switchable coarse view computed on demand.

The downstream usage sites the plan lists (MCP `getEdge` serialization, `codegraph status`
per-tier counts, `codegraph_context` detail-level handling) are **not** wired here — they
belong to the CLI/MCP layer and are deferred to those sub-phases / P2.4. P0.4d delivers
the type and the function; consumers adopt it incrementally.

## Verification

- `npx tsc --noEmit` — clean.
- `deriveConfidenceTier` covered by `__tests__/p04-edge-infra.test.ts` (the
  "defaultConfidence / deriveConfidenceTier" block).
