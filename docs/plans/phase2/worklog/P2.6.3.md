# P2.6.3 — CHANGELOG `## [0.9.0]` + version bump

**Status**: complete
**Date**: 2026-05-24
**House rules**: per CLAUDE.md, do NOT run `npm publish`, `git push`, `git tag`, or `./scripts/release.sh` — those are user-driven publish actions. This sub-task only edits files.

## Changes

### `CHANGELOG.md`

Prepended a `## [0.9.0] - 2026-05-24` block at the top (above existing `## [0.8.0]`). Sections:

- **Added** — stale-aware sync, `codegraph scip-refresh`, status command enhancements, `Node.stale` / `Edge.stale` public field, public API additions, scheduler templates.
- **Changed** — `getStats` behavior change (excludes hidden-stale), edge-query visibility filtering, fuzzy search filter, schema bump 6→7.
- **Performance** — measured perf numbers from P2.6.1 (6.5ms per call at 5000 hidden, well below 200ms budget).
- **Notes** — sync's new lifecycle category surfaced via `status`; cross-process refresh-lock gap documented.

Added link reference `[0.9.0]: https://github.com/colbymchenry/codegraph/releases/tag/v0.9.0` at end of the 0.9.0 block, mirroring the existing `[0.7.12]` pattern.

**Note**: The `## [0.8.0]` section that already existed (P1's release entry) has NO link reference in the file — likely because P1 was never published as v0.8.0. Not adding one as part of P2 work; that's the user's call when they bundle the releases.

### `package.json`

`"version": "0.7.12"` → `"version": "0.9.0"`.

Jumps minor by 2 (0.7 → 0.9). Rationale: 0.8.0 is the P1 CHANGELOG entry's tag, but `package.json` was never bumped at that time (the publish flow wasn't run). P2 ships as 0.9.0 — consistent with the existing CHANGELOG sequence (0.8.0 P1 was released, 0.9.0 P2 follows).

If the user prefers to bundle P1 + P2 in a single 0.9.0 release, the existing `## [0.8.0]` block can be folded into 0.9.0 manually before publish — out of scope here.

## Writing-style notes (per CLAUDE.md house rules)

- **User-perspective**: each bullet leads with observable behavior / capability, not implementation detail. E.g. "editing a SCIP-covered file no longer destroys its SCIP-precision data" instead of "introduced markScipFileStale helper."
- **Explicit behavior-change callouts** for `getStats` and edge-query filtering — both are observable to programmatic consumers. CHANGELOG has migration breadcrumbs (`getStatsIncludingStale`) inline.
- **No raw method names in headlines** — they appear in body text where users need them to act on the change.

## Validation

- `npm run build` succeeds with the new version. `tsc` clean. Build output shows `@colbymchenry/codegraph@0.9.0`.
- CHANGELOG.md renders correctly (markdown structure intact, link references in place).

## Risk callouts

- **`## [0.8.0]` lacks a link reference**. If the user publishes 0.8.0 + 0.9.0 separately, they should add `[0.8.0]: https://github.com/colbymchenry/codegraph/releases/tag/v0.8.0` near the 0.9.0 link. If they fold into a single release, this is moot.
- **Version jump 0.7.12 → 0.9.0** skips 0.8.x. Semver-wise fine (no constraint forbids it), but worth a note in the release announcement if any.
- **No `package-lock.json` bump committed** — would update automatically on the next `npm install`. Not blocking.

## What's NOT in this sub-task

- `git commit`, `git tag`, `git push`, `npm publish`, `./scripts/release.sh` — all user-driven per CLAUDE.md.
- GitHub Release creation — `scripts/release.sh` extracts the 0.9.0 block automatically when the user runs it.
